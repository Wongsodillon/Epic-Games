const sideButtons = document.querySelectorAll('.side-content');
sideButtons.forEach(button => {
    button.addEventListener("click", (e) => {
        // document.querySelector(".active").classList.remove("active")
        // button.classList.add("active")
    })
})
